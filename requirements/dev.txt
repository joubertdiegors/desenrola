# Desenrola - dependencias de desenvolvimento
# Instalar com: pip install -r requirements/dev.txt

-r base.txt

pytest==9.1.1
pytest-django==4.14.0
ruff==0.16.7

# Congela o relogio nos testes de data. Sem isto, uma suite longa que
# atravessa a meia-noite compara o "hoje" guardado no import do modulo
# com o "hoje" que a aplicacao calcula na requisicao -- e falha sem que
# exista defeito nenhum. Ja aconteceu.
freezegun==1.5.5
django-debug-toolbar==8.0.0

# Compila .po -> .mo no Windows sem precisar do gettext instalado
polib==1.2.0
